import React from "react";

import Grid from "../../grid";
import projectDetails from "../../../constants/projectDetails";

const Projects = () => {
  return <Grid items={projectDetails} />;
};

export default Projects;
